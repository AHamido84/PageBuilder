# Seven Eleven Trading — Project Handoff / Continuation Notes

**Read this first in a new session** to pick up where this one left off. This file is intentionally *not* a design document — it's operational context: what exists, what's verified, what's not done, and how to keep going.

---

## What this project is

A production website + enterprise CMS for **Seven Eleven Trading**, a real Jeddah-based wholesale food distributor (verified via LinkedIn: founded 2023, 51–200 employees, frozen-food specialty, serves hotels/restaurants/catering/hospitals/wholesale). No fabricated certifications, awards, or stats appear anywhere in the build — only verified facts or clearly-labeled placeholders.

Built in 6 phases so far, each a separate user-issued command:
- **Phase 0** — architecture analysis & plan (see `PROJECT-PLAN.md`)
- **Phase 1** — database, auth, RBAC, foundational admin CRUD
- **Phase 2** — premium public website (design system + all public pages)
- **Phase 3** — enterprise CMS (every remaining admin module)
- **Phase 4** — real visual Page Builder: 31-block registry, drag-and-drop canvas, responsive per-breakpoint styling, draft/publish snapshot versioning with restore (see below)
- **Phase 5** — product catalog depth (specs/certifications/related products/multi-document media), a hardened Media Library (MIME+extension+size validation, image optimization, SVG sanitization), split product inquiries (Request Info vs. Request Quote), public Blog + FAQ, and a real demo-content seed script (see "Product Catalog & Media architecture" below)
- **Post-Phase-5** — switched the Media Library's storage backend from the local filesystem to Vercel Blob (see "Media pipeline" below), then pushed and deployed everything through Phase 5 to production
- **Phase 6** — the business/SEO layer: real per-page `generateMetadata` (title/description/canonical/OG/Twitter/robots/hreflang) on every public route, JSON-LD structured data (Organization/Product/Article/Breadcrumb/FAQ), `/sitemap.xml` + `/robots.txt`, a basic `Redirect` system, two new lead inquiry types (Become a Customer, Sales Inquiry) with a tabbed selector on `/contact`, lead assignment + richer filters + pagination in the admin, configurable GA4/GTM/Meta Pixel injection, WhatsApp CTA + Google Maps embed rendering (both conditional on real admin-configured values, never invented), and an email-notification architecture (SMTP via `nodemailer`, no-ops gracefully if unconfigured) — see "SEO, Structured Data & Business Integrations" below
- **Post-Phase-6** — homepage migrated from hardcoded React into a real Page Builder page (see "Homepage architecture" below)
- **Phase 7** — full production QA/security/performance audit: build made fully lint/typecheck/build-clean (13 pre-existing lint errors fixed), RBAC-audited (all 76 admin server actions confirmed authorization-gated), upload validation hardened (magic-byte checks added for PDF/video, previously trusted client-supplied MIME type alone), a real DB transaction-timeout bug fixed in the Page Builder's save path, branded error boundaries added (`error.tsx`/`global-error.tsx`, previously nonexistent), and several real bugs found via live browser testing and fixed — see "Phase 7 QA audit" below for the full list and what wasn't covered
- **Post-Phase-7, session A** — dynamic Menus + dynamic/editable Contact page (gap #1 below, resolved) — **pushed to GitHub and deployed to production** (commit `779893d`), prod DB seeded. See "Post-Phase-7 work" below.
- **Post-Phase-7, session B** — a from-scratch "Ultra Premium" visual/motion redesign (6 phases, complete), a full CMS-driven "Hero Media System" (image/video/slideshow modes, all 4 phases complete), and — same thread, later prompt — a "Premium Cinematic Hero System" (organic frame/mask system, decorative typography, pointer parallax, a new CMS-driven Product Composition mode; all 4 phases complete and verified live). **Verified locally (build/lint/typecheck clean, live-browser-tested both locales) but NOT pushed to GitHub or deployed.** Several real bugs found and fixed along the way. See "Post-Phase-7 work" below — **read this before doing anything else if continuing this thread.**

Also: this app is deployed to production on Vercel at `https://seven-eleven-trading.vercel.app`, on a separate Neon branch (`prod`) from the dev database described below — see "Production deployment" further down.

Everything through Phase 6 is committed, pushed to **https://github.com/AHamido84/PageBuilder** (branch `main`), and deployed to production, including its migration (`20260810082045_phase6_seo_leads_integrations`) on the `prod` Neon branch. Confirm `git status`/`git log` and `prisma migrate status` against both branches for the current state before assuming otherwise.

---

## Environment setup (things that bit us before)

- **Node.js**: installed via `winget` (v24.19.0) at `C:\Program Files\nodejs`. This shell's PATH does **not** auto-refresh across PowerShell invocations — every command needs `$env:Path += ";C:\Program Files\nodejs;$env:APPDATA\npm"` prepended, or use the existing `.claude/launch.json` configs which already bake this in via `cmd.exe /c "set PATH=...&& npm run dev"`.
- **Docker Desktop never worked** in this environment (GUI first-run dialog can't be clicked headlessly). We gave up on local Postgres and used a **cloud Neon Postgres** instance instead.
- **Database**: PostgreSQL on Neon. Connection string lives in `.env` (`DATABASE_URL`) — **not** committed, not reproduced here. If starting fresh and `.env` is missing, you'll need the user to supply it again (see Phase 1 conversation for the original setup flow).
- **Media storage**: Vercel Blob (`BLOB_READ_WRITE_TOKEN`, in `.env.local`, gitignored). If missing after a fresh checkout, the project is already linked (`.vercel/project.json`) — run `npx vercel env pull .env.local` (needs `vercel login` / an authenticated CLI session first) to fetch it, or `npx vercel blob list-stores` to confirm the store still exists. See "Media pipeline" under Phase 5 below.
- **Admin login**: credentials are in `.env` as `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD`. Log in at `/admin/login`.
- **Prisma is pinned to v6** (not v7) — v7's driver-adapter-only config was too new/unstable when this was built. Don't upgrade without deliberately re-testing the migration flow.
- **`prisma migrate dev` doesn't work in this non-interactive shell** (needs a TTY to confirm data-loss warnings). The working pattern used throughout: `prisma migrate diff --from-url <url> --to-schema-datamodel prisma/schema.prisma --script` → manually write the SQL into a new `prisma/migrations/<timestamp>_<name>/migration.sql` file (avoid PowerShell `Out-File`/redirection — it adds a UTF-8 BOM that breaks Postgres; use the `Write` tool instead) → `prisma migrate deploy` → `prisma generate`.
- **Neon occasionally cold-starts/drops connections** (P1001 errors) after idle periods — this caused at least one false "bug" during testing that was actually just a transient reconnect. Retry before assuming a real issue.
- **A new RBAC resource added to `src/lib/rbac/permissions.ts`'s `RESOURCES` array needs the seed script re-run against every environment, not just a schema migration.** `prisma migrate deploy` only applies `prisma/schema.prisma` changes (new tables/columns) — it does **not** insert the new `Permission`/`RolePermission` rows that `prisma/seed.ts` seeds from `ALL_PERMISSIONS`/`DEFAULT_ROLE_PERMISSIONS`. Hit this for real deploying Phase 5's `faqs` resource to prod: the migration ran fine, but `/admin/faqs` 500'd with `Forbidden: missing permission faqs:read` until `npx tsx prisma/seed.ts` was run against the prod `DATABASE_URL` too. `prisma/seed.ts` is fully upsert-based (`update: {}` on every upsert) so it's safe to re-run against a populated database at any time — it only adds rows that don't exist yet, never overwrites existing permissions, role descriptions, or (critically) the admin user's password. Same `vercel env pull` + override-`$env:DATABASE_URL` pattern as the migration workaround above.
- **The dev Neon database was found completely empty partway through Phase 6** (all tables present matching the current schema, but zero rows in every table, and no `_prisma_migrations` tracking table at all) — signature of a `prisma db push` (possibly `--force-reset`) having been run against it outside this session, not a `prisma migrate reset` (which would have auto-reseeded the admin user via `prisma/seed.ts`, and it hadn't). Recovered by: `prisma migrate resolve --applied <name>` for each of the 7 pre-existing migrations (to rebuild the tracking table matching the schema that was already there), `prisma migrate deploy` for the new Phase 6 migration, `prisma generate`, `npm run db:seed`, then `npx tsx scripts/seed-demo-content.ts` to restore the full demo catalog. If dev ever comes up empty again, that's the recovery sequence — don't assume data loss is unrecoverable before checking whether it's just a missing migration-history/seed situation.
- **Email notifications** (Phase 6): `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_FROM` are not set in this environment — `src/lib/email.ts` detects this and no-ops (logs a warning, doesn't throw), so lead submission still works end-to-end without email actually sending. Supply real SMTP credentials (any provider — Gmail, SendGrid, SES, etc. all work via SMTP) to activate.
- **Dev vs prod preview**: `.claude/launch.json` has two configs — `seven-eleven-trading` (dev, `npm run dev`) and `seven-eleven-trading-prod` (`npm run build && npm run start`... actually just `npm run start`, so build first manually). Testing was mostly done against the **prod build** since Turbopack dev-mode HMR was flaky during heavy editing (stale bundles, phantom module-not-found errors that were just cache issues).
- **Browser automation quirk**: the `computer` tool's `left_click` occasionally times out or silently no-ops on certain buttons (root cause never fully identified — possibly environment-specific). When a click seems to not register, verify via `read_page`/`get_page_text` before assuming a code bug — several "bugs" during Phase 2/3 testing turned out to be this, not real issues. A dispatched-then-separately-checked JS pattern (`javascript_tool`) was the reliable fallback for confirming React state actually changed (React's commit isn't synchronous within a scripted `dispatchEvent`, so check in a *separate* tool call, not the same one). For Page Builder canvas section-selection specifically, `.click()` dispatched via `javascript_tool` directly on the real click-capture overlay element (`.absolute.inset-0.z-10.cursor-pointer`) is far more reliable than coordinate-based `computer.left_click`.
- **Node/npm aren't on PATH in the Bash tool either** (not just PowerShell) — every Bash command needing `node`/`npm`/`npx` needs `export PATH="/c/Program Files/nodejs:$PATH"` prepended.
- **`tsx` auto-loads `.env` but not `.env.local`.** Any one-off script run via `npx tsx scripts/....ts` that needs something only in `.env.local` (e.g. `BLOB_READ_WRITE_TOKEN`) needs that variable passed explicitly (`BLOB_READ_WRITE_TOKEN="$(cat tmpfile)" npx tsx ...`), same pattern as the prod-`DATABASE_URL` override already documented above.
- **`npm run build` and a running dev/prod preview server can't share the same repo directory concurrently** — starting a build while `next start`/`next dev` is running against the same `.next` output caused the preview server to crash/become unreachable (observed directly this session). Stop the preview server (or wait for the build to fully finish) before doing the other.
- **The admin Page Builder canvas autosaves on a debounce.** A stale browser tab left open on a builder page, with unsaved edits from earlier testing, can silently overwrite a fresh database reseed a few seconds after the reseed script reports success (observed directly this session — a reseed appeared to succeed, then a background autosave from an old tab clobbered it). Close or navigate away from any open builder tab before reseeding via script, and re-verify the DB state immediately after.
- **Bracket-containing paths** (Next.js route folders like `[locale]`, `[id]`) break the `Glob` tool's pattern matching and plain `find`/quoting in Bash. Use `find "path" -iname "*pattern*"` with the literal path quoted, or read the directory via other means — don't rely on `Glob` for anything under a `[...]` segment.

---

## Tech stack

- Next.js 16.3 (App Router, Turbopack), TypeScript, React 19
- Prisma 6 + PostgreSQL (Neon)
- Custom auth: bcrypt + signed JWT session cookie (`jose`), no third-party auth library
- `next-intl` for Arabic (RTL, default)/English (LTR)
- Tailwind CSS v4 (CSS-first `@theme` config, no `tailwind.config.js`)
- Zod validation throughout server actions

---

## Design system (Phase 2)

Palette: Ink `#0B1C2C`, Paper `#F7F5F0`, Harbor `#1F4E5F`, Wheat `#C99A4B`, Frost `#DCE6E4`, Signal `#B23A2F`. Type: Archivo Expanded (display) + Public Sans (body) + IBM Plex Mono (data) + IBM Plex Sans Arabic (own voice, not a mirrored Latin face). Signature motif: SKU/temperature-class data rendered as a recurring "manifest strip" — operational data as the visual material.

---

## Full database schema (models)

`User`, `Role`, `Permission`, `RolePermission` (RBAC — 17 resources × 5 actions = 85 permissions after Phase 6 added `redirects`, 5 roles: Super Admin/Content Manager/Marketing Manager/Sales/Viewer) · `Page`, `PageSection` (+ `settings` Json, Phase 4), `PageRevision` (+ `isPublished` Bool, Phase 4 — now actually written on every Publish, see below) · `Category`, `CategoryTranslation` (+ icon, SEO) · `Brand`, `BrandTranslation` (+ logo, banner, SEO) · `Product` (+ `weight`, `dimensions`, `relatedProductIds String[]`, Phase 5), `ProductTranslation` (+ isFeatured, originCountry, packaging/storage info, + `shortDescription`/`ingredients`/`nutritionInfo`/`allergens`, Phase 5) · `Media` (+ `width`/`height` now actually populated, Phase 5) · `Menu`, `MenuItem` (links to Page/Category/Product/custom URL) · `BlogCategory`, `Tag`, `BlogPost` · `Form`, `FormSubmission` (still dormant, see gaps) · `Industry`, `Lead` (+ `inquiryType LeadInquiryType`, Phase 5; enum extended with `BECOME_CUSTOMER`/`SALES_INQUIRY`, Phase 6; `assigneeId` now actually editable via admin, Phase 6), `LeadNote` · `Certification` (schema existed since Phase 1; admin UI + product relation added Phase 5, seeded with zero rows — see gaps) · `Faq` (Phase 5) · `Redirect` (new, Phase 6: `fromPath` unique, `toPath`, `statusCode` enum, `isActive`) · `SiteSetting` (singleton, fully expanded; + `gtmId`/`metaPixelId`, Phase 6) · `SEO` (attachable to Page/Product/BlogPost/Category/Brand — now actually rendered into real `<head>` metadata on every one of those public pages, Phase 6) · `NewsletterSubscriber` · `ActivityLog`.

Product's `videos`/`documents` are separate `Media[]` relations from the pre-existing `images` gallery (Phase 5 split a single mixed image+video bucket into three distinct relations). `relatedProducts` ended up as a plain `relatedProductIds String[]` scalar array on `Product`, **not** a Prisma self-relation as originally planned — simpler to query/update (no join table, no symmetric-relation semantics to reason about) and sufficient for a curated "pick a few related SKUs" use case.

---

## Admin dashboard — routes that exist and are real

All under `/admin/*`, protected by the RBAC session + `src/proxy.ts` (Next 16's middleware replacement):

- `/admin` — dashboard with live stats
- `/admin/pages`, `/admin/pages/[id]` — page list + Details/SEO; `/admin/pages/[id]/builder` — **the real visual Page Builder** (Phase 4): full-screen editor, 31 registry-driven block types (see "Page Builder architecture" below), drag-and-drop reorder, responsive Desktop/Tablet/Mobile settings per section, undo/redo, autosave, and real draft→publish versioning with a restorable revision history
- `/admin/media` — Media Library (upload/search/filter/rename/replace/delete; Phase 5: JPG/PNG/WEBP/SVG/MP4/WEBM/PDF with MIME+extension+size validation, raster images auto-resized >2000px and re-encoded via `sharp`, SVGs sanitized on upload, `width`/`height` now actually populated)
- `/admin/menus` — Menu Builder (header/footer, nested items)
- `/admin/blog`, `/admin/blog/[id]` — Blog CMS
- `/admin/products`, `/admin/products/[id]` — Products: list has search/category/brand/status filters + pagination (Phase 5); detail is 5 tabs — Details, Specifications (weight/dimensions/packaging/storage/ingredients/nutrition/allergens, Phase 5), Media (Images/Videos/Documents via one generic `product-media-collection.tsx`, Phase 5), Related & Certifications (curated related-product picker + certification checklist, Phase 5), SEO
- `/admin/categories`, `/admin/categories/[id]` — Categories (nested tree, reorder, icon, SEO)
- `/admin/brands`, `/admin/brands/[id]` — Brands (full CRUD, logo/banner, SEO)
- `/admin/certifications`, `/admin/certifications/[id]` — Certifications (new, Phase 5: slug, name EN·AR, issuer, valid from/until, image, publish toggle; seeded with **zero rows**, see gaps)
- `/admin/faqs`, `/admin/faqs/[id]` — FAQs (new, Phase 5: question/answer EN·AR, category tag, order, publish toggle)
- `/admin/redirects` — Redirect list (new, Phase 6: from-path/to-path/status-code create form, active/disabled toggle, delete; no edit-in-place — delete and recreate to change a path, kept deliberately basic)
- `/admin/leads`, `/admin/leads/[id]` — Leads (notes, CSV export, search/filter; Phase 5: Product + Inquiry Type columns, `Lead.productId`/`inquiryType` finally populated by the public inquiry forms; Phase 6: assignment via `assigneeId` now editable, inquiry-type + assignee filters, pagination replacing the old hard `take: 200` cap, CSV export mirrors all of it)
- `/admin/forms`, `/admin/forms/[id]` — lightweight Form/FormSubmission management (see gaps)
- `/admin/users`, `/admin/users/[id]` — Users
- `/admin/roles` — Roles (read-only view of seeded RBAC matrix; no custom role builder UI yet)
- `/admin/settings` — Settings (General/Contact/Social/Hours/SEO defaults/Footer — all configurable)
- `/admin/activity` — Activity Log viewer

Shared admin UX kit at `src/components/admin/ui/`: `Modal`, `Drawer`, `ConfirmDialog` (promise-based, replaces `window.confirm`), `Toast`, `Tabs`, `SeoForm`, `MediaPickerField` (browse-or-upload, used everywhere; Phase 5: accepts SVG too), `DeleteButton`, `Pagination` (new, Phase 5 — `variant?: "dark"|"light"`, reused by both the admin product list and the public product listing).

---

## Public site — routes that exist and are real

All under `/[locale]/*` (`ar`/`en`), driven by `src/proxy.ts` + `next-intl`:

`/` (Post-Phase-6: a real Page Builder page, not hardcoded — see "Homepage architecture" below), `/products` (search + category + brand filter + pagination, Phase 5), `/products/[slug]` (Phase 5: short description, Additional Information section for weight/dimensions/ingredients/nutrition/allergens — each rendered only if populated, certification badges, document downloads, videos alongside the image gallery, curated related products falling back to same-category auto-query), `/brands`, `/brands/[slug]`, `/about`, `/solutions`, `/solutions/[segment]` (7 real segments: hotels/restaurants/catering/hospitals/wholesale/retail/food-service), `/quality-food-safety`, `/distribution-logistics`, `/blog`, `/blog/[slug]` (Phase 5 — first public consumer of the Blog CMS), `/faq` (Phase 5 — grouped by category, accordion UI), `/contact` (Phase 6: General/Quote/Become a Customer/Sales Inquiry tab selector, optional Google Maps embed if `SiteSetting.mapEmbedUrl` is set), `/privacy`, `/terms`, `/cookies`, and a **catch-all `/[...slug]`** that renders CMS pages created via the Page Builder (respects draft/publish status + admin-preview access; Phase 6: falls through to a `Redirect` lookup before 404ing).

Also (Phase 6, outside `/[locale]`): `/sitemap.xml` (`src/app/sitemap.ts` — every static route + every published Product/Brand/BlogPost/Page, both locales, with `hreflang` alternates) and `/robots.txt` (`src/app/robots.ts` — disallows `/admin` and `/api`, points at the sitemap).

Header (sticky, mega menus for Products/Solutions/Company, mobile drawer) and Footer (nav, real newsletter signup, contact from `SiteSetting`, social links including Facebook, legal links) wrap every page. A floating WhatsApp CTA (`src/components/site/whatsapp-cta.tsx`) renders sitewide **only if `SiteSetting.whatsapp` is configured** — it derives a `wa.me` link from the configured number and never invents one.

---

## Genuine gaps — read before continuing

1. ~~Public header/footer menu is still Phase 2's static nav, not wired to the new `Menu`/`MenuItem` records~~ — **resolved, Post-Phase-7 session A** (live in production). See "Post-Phase-7 work" below.
2. ~~No public `/blog` listing or `/blog/[slug]` page~~ — **resolved in Phase 5.** `/[locale]/blog` and `/[locale]/blog/[slug]` are real.
3. **The dormant `Form`/`FormSubmission` system is still untouched.** Both Phase 4's Contact Form/Quote Form page-builder blocks and Phase 5's product inquiry forms deliberately write to `Lead` (the real CRM pipeline) — a conscious choice, not an oversight; see "Page Builder architecture" below.
4. ~~`Certification` model exists in the schema with no admin UI and no public page~~ — **partially resolved in Phase 5.** `/admin/certifications` is real and products can be linked to certifications; there is still no dedicated public certifications page, and **zero certifications are seeded** (deliberate — the user's instruction was explicit: do not invent certifications; the feature is ready to use the moment the user supplies real, verified certification data).
5. ~~`PageRevision` model exists but nothing writes to it~~ — **resolved in Phase 4.** Every Publish now snapshots into `PageRevision` (`isPublished` flag marks the live one), and Restore is real (see below).
6. **No custom role/permission builder UI** — `/admin/roles` is read-only; roles are seeded, not editable via UI.
7. **No automated test suite** (Vitest/Playwright). All verification so far has been manual/scripted against a real build and real database — thorough, but not a committed regression suite.
8. ~~Phase 5's schema migration only applied to dev, code committed locally only~~ — **resolved.** Pushed to `main` and deployed; migration `20260810015850_product_catalog_v2` has been applied to the `prod` Neon branch via `prisma migrate deploy`.
9. ~~Uploaded media written to the local filesystem, won't persist on Vercel~~ — **resolved.** `src/lib/media-upload.ts` now uploads to a **Vercel Blob** store (`seven-eleven-trading-media`, public access) instead of `public/uploads/`. See "Media pipeline" under Phase 5 below for details. `public/uploads/` may still contain leftover dev-only files from before this switchover — harmless, gitignored, safe to ignore or delete.
10. **Test data lives in the real (dev) database** (never cleaned up, always flagged to the user, never auto-deleted):
    - Category "Frozen Poultry" (`frozen-poultry`)
    - Product "Whole Frozen Chicken" (`FP-1001`, published, with real packaging/storage/origin data)
    - Lead "Ahmed Al-Otaibi"
    - Leads "Fatima Al-Zahrani" (`BECOME_CUSTOMER`, assigned to Super Admin, status `CONTACTED`) and "Omar Bakr" (`QUOTE`, linked to product `BP-6004`) — created while end-to-end testing Phase 6's new inquiry types and lead assignment/filters. There's no lead-delete admin action (matches CRM semantics — status change, not deletion), so these stay unless the user wants the DB edited directly.
    - Brand "Nordic Foods" (`nordic-foods`)
    - Page `/test-cms-page` (published, one Text section — this predates Phase 4's registry; its `TEXT` type is gone from the new registry, so it'll render as an empty/skipped section now. Delete and recreate through the new builder if it needs to stay.)
    - Page `/page-builder-e2e-test` (Phase 4's end-to-end test page — Hero/Rich Text/Feature Cards/Accordion sections, published). Same disposable-test-data status as the rest of this list.
    - Plus Phase 5's real demo catalog from `scripts/seed-demo-content.ts` (19 categories, 6 brands, 23 products, 6 blog posts, 10 FAQs, 0 certifications) — see "Demo content" below. This is meant to look like real catalog data (it's what a visitor/reviewer will see), unlike the items above which are throwaway single-record test fixtures.
    - One test SVG (`public/uploads/2026/08/…svg`) uploaded while verifying the Phase 5 SVG-sanitizer fix — left on disk, not committed (see `.gitignore`).
    - The user has been asked multiple times whether to clean up the single-record test fixtures and hasn't answered yet — worth asking again or just doing it (low-risk, clearly-labeled test data) if it comes up. **Note: the production Vercel deployment's database has no seed/demo catalog data** — it's only the dev Neon branch that has the items above. Prod is not necessarily *empty* though (see gap #15) — check `/admin` before assuming. **As of Phase 6's deploy, prod also has one real test lead**: "Layla Mansour" (`SALES_INQUIRY`, `layla@example.com`), submitted live against production to verify the new inquiry-type pipeline end-to-end. Same no-delete-action caveat as the dev test leads.
    - **The entire dev database was rebuilt from scratch mid-Phase-6** (see "Environment setup" above) — everything in this list as of this writing was recreated via the seed scripts, not carried over from earlier phases. Functionally identical, just worth knowing the row ids all changed.
    - Leads "QA Test Contact" (`SALES_INQUIRY`) and "QA Product Inquiry" (`INFO`, linked to product `BP-6004`) — created during Phase 7's end-to-end forms QA (real submissions through the actual `/contact` and product-inquiry forms, verified all the way to the admin leads list). Same no-delete-action caveat as the other test leads above.
11. ~~Not every `SiteSetting` field is consumed on the public site yet~~ — **partially resolved in Phase 6.** `whatsapp` (floating CTA), `mapEmbedUrl` (`/contact` iframe), and `socialLinks.facebook` (footer, was silently dropped before) now render. Still unconsumed: logo/favicon replacing the text wordmark, business hours display.
12. ~~The hardcoded marketing homepage was still hardcoded React, not a Page Builder-driven `Page`/`PageSection` record~~ — **resolved, Post-Phase-6.** See "Homepage architecture" below.
13. **The `Redirect` system only covers the CMS catch-all route (`/[locale]/[...slug]`)**, not built-in routes like `/products`, `/blog`, etc. — deliberate, matching the spec's "basic redirect system" scope; a request for `/products/some-slug` that 404s inside that route's own logic never reaches the redirect lookup. If a built-in route ever needs redirect coverage, that's a bigger change (likely back to evaluating middleware + an edge-safe DB read, which was avoided this round specifically because `src/proxy.ts` currently does zero DB queries and adding one would be new territory — see the redirect implementation note below).
14. **No public certifications page and GA4/GTM/Meta Pixel/SMTP have no real values configured** — all deliberate placeholders per the "don't invent" instruction (see gap #4 for certifications, and "Analytics" below for tracking IDs). Everything is wired and tested to work the moment real values are supplied via `/admin/settings` or `SMTP_*` env vars.
15. **The user works directly in this same dev server / prod admin panel in their own browser, concurrently with agent sessions.** During the Vercel Blob migration testing, an automated browser test collided with the user's own real activity: the user uploaded a real `Logo.png` through `/admin/media` (or `/admin/settings`) at the exact moment an automated test script was mid-flow on that same Media record, and a DOM-selector bug in the test script (`document.querySelector('input[type="file"]')` matching the wrong hidden file input while a details drawer was open) caused the test to **replace, then delete** the user's real logo upload. The blob was permanently gone from Vercel Blob storage by the time this was caught (confirmed via `vercel blob list`) — no undo available. **Lesson for future sessions**: don't assume you have exclusive control of a shared dev server or the prod admin panel; check the Activity Log (`/admin/activity`) for actions you didn't take before and after any live UI testing, prefer scoping file-input selectors precisely (e.g. by a stable `data-testid`, not a bare `input[type="file"]` query) when more than one such input can be mounted at once, and flag to the user immediately if anything unexpected shows up mid-test rather than continuing. If the user ever needs their logo re-uploaded, that's on them to supply — it wasn't recoverable from this end.

---

## Page Builder architecture (Phase 4)

Registry-driven, not hardcoded: `src/lib/page-builder/registry.ts` assembles `BLOCK_REGISTRY` from 8 category files under `blocks/` (31 block types total). Every block is one `BlockDefinition` — `{ type, label, category, icon, dataSchema (Zod), defaultData, defaultSettings, Edit, Render, canvasPreview? }`. The **same `Render` component powers both the admin canvas and the public site** via `SectionShell` — there is no separate "renderer" to keep in sync by hand.

**Two gotchas that cost real debugging time, if extending this:**
- **RSC boundary**: `block.Render` must always be used as JSX (`<block.Render .../>`), never called as a plain function (`Render(props)`) — the latter breaks for any block that's a `"use client"` component (i.e. almost all of them) with a cryptic React #418/#441 hydration error. Same rule for `Edit`: never export a *factory function* that returns a component from a `"use client"` file and call that factory from a server module (e.g. a `blocks/*.ts` registry file) — calling a client-exported function from server code at module-eval time throws "Attempted to call X() from the server but X is on the client." `layout/columns.tsx` and `interactive/accordion.tsx` show the fix: export the concrete pre-bound components directly (`TwoColumnsEdit`, `ThreeColumnsEdit`, ...), never a `makeXEdit(count)` factory.
- **Commerce blocks** (Product Grid/Carousel, Category Grid, Brand Grid) do live Prisma queries, so their real `Render` must be an async Server Component — which can't mount inside the client-side admin canvas. They get a `canvasPreview` (a small static summary card) instead; `canvasPreview`, when present, is what the canvas shows, and `Render` is reserved for the public site.

**Responsive settings**: `PageSection.settings` (`{ desktop, tablet, mobile, background, animation }`) resolves to Tailwind classes via **fully literal lookup tables** in `style-tokens.ts` (e.g. separate `PADDING_Y_BASE`/`_MD`/`_LG` objects) — never string-concatenated (`` "md:" + token ``). Tailwind v4's JIT only detects literal class substrings physically present in source; a concatenated class silently produces no styling in the production build while looking fine in dev. Don't "simplify" these tables into a prefix-builder helper.

**Draft vs. published**: `PageSection` rows are always the live *working draft*. Publishing snapshots the current sections into a new `PageRevision` (flips `isPublished`, unflips the previous one — enforced by a hand-added partial unique index, `PageRevision_pageId_published_key ... WHERE "isPublished" = true`, that **isn't representable in `schema.prisma`'s DSL** — a future `prisma migrate diff` won't know about it and may propose dropping it; keep it in the next migration's SQL by hand if that happens). Anonymous visitors on a `PUBLISHED` page read that snapshot; logged-in admins previewing always see the live working draft. Restoring an old revision copies its content back into the live working draft **without** auto-republishing (a safety snapshot of the pre-restore draft is taken first) — the admin reviews it and must hit Publish again.

**Two real race-condition bugs found and fixed during Phase 4's own end-to-end testing** (both in `page-builder-shell.tsx`), worth knowing about if autosave ever seems to silently revert content: (1) the autosave-scheduling `useEffect` used to fire on the component's very first mount too, arming a stale-data save timer immediately after every page load; (2) the `beforeunload` handler unconditionally called `save()` with stale in-memory `sections` — since `window.location.reload()` (used after a Restore, so the client re-reads fresh server data) triggers `beforeunload`, this could fire *after* a restore's DB write and silently overwrite it back to the pre-restore content. Both are fixed (mount-skip flag + a `suppressAutosaveRef` set before any deliberate reload) — this is exactly the kind of bug that only shows up by actually clicking through the flow end-to-end, not by code review alone.

**Browser-automation note**: the `computer` tool's `left_click_drag` needs a prior `screenshot` to calibrate coordinates, and fails outright if the Browser pane hasn't been composited yet (fresh preview tabs sometimes need one throwaway `screenshot` call before drag works). dnd-kit's `PointerSensor` also could not be reliably driven via manually-dispatched synthetic `PointerEvent`s (tried `document.dispatchEvent`, direct `props.onPointerDown` invocation, custom `pointerId`s — none activated the sensor); once the pane was composited, the real `computer{action:"left_click_drag"}` tool worked perfectly. If drag-and-drop testing seems stuck, try a screenshot first before concluding it's a real bug. **Confirm-dialog note (found testing the homepage migration below)**: `RevisionHistoryPanel`'s Restore button opens a promise-based `ConfirmDialog` before actually calling `restoreRevisionAction` — a single `computer.left_click`/`.click()` on the row's "Restore" only opens that dialog, a *second* click on the dialog's own "Restore" button is required to actually confirm. Easy to mistake for a broken restore action if you only check after the first click.

---

## Homepage architecture (Post-Phase-6)

The marketing homepage (`src/app/[locale]/page.tsx`) is now a real Page Builder page, not hardcoded React — gap #12 resolved. It uses the *exact same* `Page`/`PageSection`/`PageRevision` pipeline as every other CMS page (reuses `SectionRenderer`, `buildMetadata`, and the same draft/publish/revision-history machinery Phase 4 already built generically) — this was mostly a matter of pointing the homepage route at that existing pipeline, not building a new one.

**Reserved slug**: the homepage's `Page` row uses `slug = "__homepage__"` (the constant lives in `src/lib/page-builder/homepage.ts`, `HOMEPAGE_SLUG` — imported everywhere this matters, don't hardcode the string again). Deliberately not a normal-looking slug like `"home"`, so it's never independently reachable through the `[...slug]` catch-all as a duplicate URL for the same content (that route explicitly 404s on this exact slug). `/admin/pages` and the Page Builder toolbar both special-case this slug to display "Homepage (/)" and link Preview at `/en`/`/ar` (not `/en/__homepage__`). `deletePageAction` and `updatePageSlugAction` both reject any attempt to delete or rename this page — a one-click way to break the site's root was cheap to guard against.

**Seeding**: `scripts/seed-homepage.ts` (same rerunnable-upsert pattern as `seed-demo-content.ts`) creates the 12 default sections — Hero, Image+Text intro, Category Grid, Product Grid, Brand Grid, two Icon Cards blocks ("Why Seven Eleven" and a Solutions teaser linking the 7 real `/solutions/[segment]` pages), Timeline (Distribution & Logistics steps), a CTA (Quality & Food Safety teaser), News Grid (latest blog posts), a final CTA, and a Contact Form — then publishes it (creates the first `PageRevision`). All copy reuses the pre-existing, already-reviewed `home`/`solutions` i18n namespace strings — nothing invented. **Deliberately not seeded: a Statistics section** — no real numbers exist yet (same "don't invent" rule as certifications, gap #4); the block is fully available for the admin to add once real data exists. **This has only been run against dev** (`npx tsx scripts/seed-homepage.ts`) — prod's homepage is still whatever was there before this change until the same script (or the equivalent admin action) is run against prod's `DATABASE_URL`, same override pattern as every other one-off prod script in this project.

**Small block-registry extensions**, all additive/backward-compatible (`.optional()`/`.default()` in zod schemas, so pre-existing sections on other pages using these block types are unaffected): Hero gained a second CTA (`ctaLabel2`/`ctaUrl2`); Image+Text gained an optional CTA button; Icon Cards items gained an optional `link` (used by both new Icon Cards instances above); the shared Contact Form/Quote Form block (`CONTACT_FORM`/`QUOTE_FORM`, component in `blocks/forms/lead-form.tsx`) gained an admin-editable `inquiryType` select (`GENERAL`/`QUOTE`/`BECOME_CUSTOMER`/`SALES_INQUIRY`) wired through to `submitLead()` via a hidden form field — previously always submitted as `GENERAL` regardless of which block/copy was shown, so Quote Form silently mislabeled its leads until now. A new `NEWS_GRID` commerce block was added (`blocks/commerce/news-grid.tsx` + `news-grid-render.tsx`, modeled directly on `ProductGridRender`'s live-Prisma-query pattern) — auto mode is latest-posts-with-optional-category-filter only, no "featured" mode, since `BlogPost` has no `isFeatured` column (unlike `ProductTranslation`, which does) and this didn't seem worth a schema migration. `reference-data-context.tsx` gained a `blogCategories` list (populated in the Page Builder's `page.tsx` loader) for the News Grid's category-filter dropdown.

**Two real bugs found and fixed while actually testing this end-to-end** (not caught by `tsc`/`eslint`/`next build`, only by clicking through the real admin flow against the real Neon dev database):
1. **`saveDraftAction` (`actions.ts`) could throw P2028 ("Transaction already closed")** on any page with enough sections that N sequential per-row `upsert()` calls inside one interactive `$transaction` exceeded Prisma's 5s default timeout against Neon's per-query latency. Every pre-existing CMS page apparently had few enough sections to stay under that ceiling by luck; the homepage's 12 sections didn't. Fixed by splitting new rows into a single `createMany` (one round trip regardless of count) and only per-row `update()` for rows that already exist, plus an explicit `{ timeout: 20000 }` on the transaction as a safety margin. This fix applies to *every* page's autosave/save, not just the homepage — if a heavily-sectioned page ever felt "stuck on Saving" before, this was almost certainly why.
2. **The Contact Form/Quote Form block's field labels ("Name", "Company", "Email", "Phone", "Message", the submit/sending/thank-you strings) were hardcoded English**, so the Arabic homepage's embedded form rendered English labels mid-RTL page. Fixed with a small inline `FORM_LABELS` EN/AR map in `lead-form.tsx` keyed off the block's `locale` prop (the same pattern as `resolveHref`/`ICON_OPTIONS` elsewhere in the registry — a small inline dictionary, not a new i18n subsystem). This bug predates the homepage work (it affected every existing use of these blocks on any page, in Arabic) but only got noticed because the homepage is the first place these blocks were actually re-tested in Arabic end-to-end.

**Verified live** (real admin workflow against the dev Neon DB, not just code review): homepage renders correctly in both `/en` and `/ar` with real DB-driven content (categories/products/brands/blog posts) and zero console errors; Page Builder canvas loads and displays all 12 sections with working Duplicate/Hide/Delete controls; editing English content and Arabic content are independent (verified by DB inspection, not just the UI) with no cross-contamination; an unsaved/unpublished edit does **not** appear on the public site (draft isolation); Publish makes it live in both locales; Revision History correctly lists all revisions and Restore correctly reverts the working draft (confirmed via direct DB read of the `PageSection` rows before/after, not just the UI) — a subsequent Publish is required to make a restored draft live again, matching the documented "restore doesn't auto-republish" behavior.

---

## Product Catalog & Media architecture (Phase 5)

**Products stay simple, unlike pages**: Page Builder's draft/working-copy vs. published-snapshot split does **not** apply to `Product`. A product is just `isPublished: Boolean` — editing a published product changes it live immediately, same as Categories/Brands/Blog. This is intentional (products aren't a layout-authoring surface with an undo/preview workflow; they're catalog records), not a leftover gap.

**`relatedProductIds` is a scalar array, not a relation**: `Product.relatedProductIds String[] @default([])` holds raw product ids picked via the admin's Related & Certifications tab. This was a deliberate simplification from the original plan (a `Product[] @relation("RelatedProducts")` self many-to-many) — a scalar array needs no join table and no symmetric-relation bookkeeping, and the picker only needs "show me these specific other products," never a query in the reverse direction. If a product referenced in the array gets deleted, its id just becomes a harmless dangling entry (the detail-page query filters to `{ id: { in: relatedProductIds } }`, which silently ignores ids that no longer resolve) — no cascading delete/foreign-key error to handle.

**SVG upload security bug found and fixed during Phase 5's own testing** (`src/lib/media-upload.ts`): the first version of `sanitizeSvg()` passed `allowedAttributes: false` to `sanitize-html`, which means "allow every attribute on every tag" — the exact opposite of the intended allowlist, and it let `onload`/`onclick`/etc. straight through while only the `<script>` tag itself got stripped. Caught by a direct test payload (`<svg onload="alert(1)"><script>alert('xss')</script><circle onclick="alert(2)"/></svg>`), not user-reported. Fixed with an explicit `SVG_SAFE_ATTRIBUTES` allowlist (presentation/geometry attributes only) passed as `allowedAttributes: { "*": SVG_SAFE_ATTRIBUTES }`. If touching SVG handling again: `allowedAttributes: false` in `sanitize-html` is a footgun that reads like "no attributes allowed" but means the opposite — always pass an explicit list.

**Media pipeline**: `saveUploadedFile()` validates both MIME type *and* derives the file extension from that validated MIME type (never from the client-supplied filename, so a malicious extension can't reach disk) *and* enforces a per-type size cap (images 5MB, SVG 1MB, PDF 10MB, video 50MB). Raster images (`jpeg`/`png`/`webp`) go through `sharp`: resized down (preserving aspect ratio) if wider than 2000px, re-encoded at quality ~82, and their final `width`/`height` returned and persisted on `Media` — previously-empty columns. SVGs are sanitized (above) but not resized (vector). Video/PDF pass through unmodified.

**Storage backend (added after Phase 5)**: uploads are written to a **Vercel Blob** store, not the local filesystem — `saveUploadedFile()` calls `put()` from `@vercel/blob` with `access: "public"` and a UUID-based pathname (`uploads/YYYY/MM/<uuid>.<ext>`, `addRandomSuffix: false` since the UUID already guarantees uniqueness), and `deleteUploadedFile()` calls `del()` on the stored blob URL. This was required because Vercel's serverless functions have an ephemeral filesystem — anything written to `public/uploads/` in production would vanish after the instance recycled (this was gap #9, now resolved). The store is named `seven-eleven-trading-media`, connected to the Vercel project, and its `BLOB_READ_WRITE_TOKEN` is set in all three Vercel environments (Production/Preview/Development) plus pulled into local `.env.local` for dev — `next dev` reads both `.env` and `.env.local` automatically, no manual wiring needed. `next.config.ts`'s CSP `img-src`/`media-src` directives allow the `*.public.blob.vercel-storage.com` domain. `deleteUploadedFile()` no-ops on any URL that isn't `http(s)://` (guards against pre-migration `/uploads/...`-style Media rows, though none exist in prod — prod's DB was empty when this was built).

**Inquiry split**: `contact-form.tsx` renders two submit buttons using the native HTML multi-submit-button pattern — `<button type="submit" name="inquiryType" value="INFO">` and `value="QUOTE"` — so only the clicked button's name/value pair reaches `FormData`, giving Request Info vs. Request Quote without any extra client-side state. `submitLead()`/`leadSchema` (`src/lib/leads/submit-lead.ts`) accept an optional `productId`, looked up defensively (doesn't throw if the id doesn't resolve) so a stale product reference can't break lead submission. (Phase 6 extended this same component with a `showTypeSelector` prop for the standalone `/contact` page — see below.)

**Demo content**: `scripts/seed-demo-content.ts` (run via `tsx`, standalone from `prisma/seed.ts` which stays focused on auth/RBAC bootstrap) is real and rerunnable — it upserts by slug/SKU, so running it again updates rather than duplicates. Current counts in the dev database: 19 categories (with subcategories), 6 brands, 23 products (all new Phase 5 fields populated with realistic-but-generic bilingual copy), 6 blog posts, 10 FAQs, **0 certifications** (deliberate — see gap #4). Every fabricated detail in this content is a generic product/operational attribute (weight, packaging, typical allergens, etc.); no certifications, awards, partnerships, revenue figures, customer counts, or geographic/official company claims were invented anywhere, per the user's explicit instruction.

---

## SEO, Structured Data & Business Integrations (Phase 6)

**Metadata**: `src/lib/seo/metadata.ts` exports `buildMetadata()`, called from a `generateMetadata()` export in **every** public route (`src/app/[locale]/**/page.tsx`, all 18 of them, plus the `[...slug]` catch-all). It takes a locale, a locale-stripped `path`, an optional per-entity `SEO` record (Product/Brand/BlogPost/Page all have one via the `seo SEO?` relation), and a translated fallback title/description for routes that don't have a dedicated SEO record (the ~9 hardcoded static pages — about, solutions, contact, legal pages, etc. — reuse existing `next-intl` copy for their fallback rather than needing new admin-editable fields, since converting those into CMS-backed entities was explicitly out of scope per gap #12). It resolves: custom title (falls back to `"{page title} — {site name}"` via `title: { absolute }`, not the layout's `title.template`, so a custom SEO title is used byte-for-byte, never re-templated), description, canonical URL (`SEO.canonicalUrl` override or computed from locale+path), `alternates.languages` for `en`/`ar`/`x-default` (this **is** the hreflang implementation — Next.js emits `<link rel=alternate hreflang=...>` tags directly from this field, no separate hreflang code needed), Open Graph (`title`/`description`/`url`/`siteName`/`locale`/`type`/`images` from `SEO.ogImage` or `SiteSetting.defaultOgImage`), Twitter card, and `robots` (`SEO.noIndex`).

**Structured data**: `src/lib/seo/structured-data.ts` exports plain builder functions — `organizationSchema()` (rendered once, sitewide, in `src/app/[locale]/layout.tsx`), `productSchema()` (product detail page; **deliberately omits `offers.price`** — this is a B2B wholesale catalog with no public list price, and inventing one would violate the project's no-fabrication rule; `availability` is included, `price` is not), `articleSchema()` (blog post detail), `breadcrumbSchema()` (product detail, brand detail, solutions segment, blog post), and `faqSchema()` (the `/faq` page — this replaced an inline ad-hoc JSON-LD object that already existed there pre-Phase-6, now goes through the shared builder for consistency). `jsonLdScript()` `JSON.stringify`s the payload and escapes `<` to `<` so untrusted DB content (product names, blog content, etc.) can never prematurely close the `<script>` tag; rendered via the `<JsonLd data={...} />` component (`src/components/site/json-ld.tsx`), which accepts either one schema object or an array.

**Sitemap/robots/hreflang**: `src/app/sitemap.ts` (must have `export const dynamic = "force-dynamic"` — seeded a real Neon-cold-start build failure the first time without it, since sitemap generation otherwise gets prerendered at build time and needs a live DB connection right then; every other DB-backed route in this app was already force-dynamic for the same reason, this one was just missed initially) enumerates every static path plus every published Product/Brand/BlogPost/Page slug, emitting **two entries per logical page** (one per locale), each carrying an `alternates.languages` map — this is what actually produces the `<xhtml:link rel="alternate" hreflang="...">` tags inside the sitemap XML that Google's guidance asks for. `src/app/robots.ts` disallows `/admin` and `/api`, points at the sitemap.

**Redirect system**: `Redirect` model (`fromPath` unique, `toPath`, `statusCode` enum `MOVED_PERMANENTLY`/`FOUND`, `isActive`) + `/admin/redirects` CRUD (create + toggle + delete, no edit-in-place). Lookup happens **inside the `[...slug]` catch-all page component** (`if (!page) { check Redirect table; redirect() or notFound() }`), not in `src/proxy.ts` middleware — deliberately, to avoid putting a Prisma query on the hot path of every single request and to avoid Edge-runtime uncertainty in this Next 16 build (`src/proxy.ts` currently does zero DB work, just JWT verification via `jose`, and this keeps it that way). Consequence: only applies to paths that fall through to the catch-all, not to built-in routes (see gap #13). `fromPath`/`toPath` are validated server-side in `createRedirectAction` (`src/app/admin/(dashboard)/redirects/actions.ts`) — `fromPath` must start with `/` and can't target `/admin` or `/api`; `toPath` must start with `/` or be a full `https://` URL — preventing `javascript:`-scheme or malformed-redirect entries.

**New lead inquiry types**: `LeadInquiryType` enum extended with `BECOME_CUSTOMER`/`SALES_INQUIRY` (existing `GENERAL`/`INFO`/`QUOTE` untouched). Rather than building 4 separate form components for Contact/Quote/Become-a-Customer/Sales-Inquiry, `contact-form.tsx` grew a `showTypeSelector` prop (only passed on the standalone `/contact` page — product-page and page-builder usages are unaffected) that renders a 4-tab segmented control wired to a hidden `inquiryType` input; all four post through the exact same `submitLead()` pipeline, differentiated only by that field. Deliberately did **not** add extra form fields (e.g. "business type") for Become a Customer — the existing free-text message field covers it, avoiding schema bloat for a rarely-critical distinction; the inquiry-type tag itself is what makes these filterable/reportable in the admin, which is the part that actually matters.

**Lead management additions**: `assigneeId` (existed in the schema since Phase 1, was never exposed in any admin UI) is now a real dropdown in the lead edit form, backed by `updateLeadAction`. The leads list gained `type`/`assignee` filters (alongside the existing `status`/search), proper pagination (`PAGE_SIZE = 25`, replacing a hard `take: 200` with no way to see anything beyond it), and the CSV export mirrors all of the same filters plus the new `Type`/`Assigned to` columns.

**Analytics**: `SiteSetting.gtmId`/`metaPixelId` (new columns; `analyticsId` already existed, now documented as specifically GA4). `src/components/site/analytics-scripts.tsx`, mounted once in the locale layout, conditionally injects each integration via `next/script` **only when its ID is configured** — nothing is ever hardcoded. GTM and GA4 are mutually exclusive by design (`ga4Id && !gtmId`): if GTM is set, GA4 is expected to be fired through GTM instead of loaded directly, avoiding double-counting. `next.config.ts`'s CSP was extended (`script-src`/`connect-src`/`frame-src`/`img-src`) to allow `googletagmanager.com`, `connect.facebook.net`, and `www.facebook.com`. **Security note**: these three ID fields get template-interpolated directly into inline `<script>` content to build each vendor's init snippet — found during this phase's own security review that the original unvalidated `z.string().max(50)` schema would have let a malicious/compromised admin account inject arbitrary JS that runs for every site visitor (not just an admin-only self-XSS, since `SiteSetting` renders sitewide). Fixed with format-anchored regexes in `updateSeoSettingsAction`'s zod schema (`^G-[A-Z0-9]+$`, `^GTM-[A-Z0-9]+$`, `^\d+$`) — this is a real constraint to preserve if these fields are ever touched again, not incidental strictness.

**Email**: `src/lib/email.ts` wraps `nodemailer` behind `sendMail({ to, subject, text })`, gated by `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_FROM` all being set — if any are missing it logs a warning and returns instead of throwing, so lead submission always succeeds regardless of email config. `submitLead()` calls it best-effort (awaited, but its own internal try/catch means a failure never propagates) to notify `SiteSetting.contactEmail` on every new lead. Not configured in this dev environment — see "Environment setup" above.

---

## Production deployment

Live at **https://seven-eleven-trading.vercel.app** (Vercel project `ahamido/seven-eleven-trading`, connected to this GitHub repo — pushes to `main` auto-deploy). Uses a **separate Neon branch** (`prod`, not the dev branch this file otherwise describes) with its own database. Production env vars (`DATABASE_URL`, `SESSION_SECRET`, `NEXT_PUBLIC_SITE_URL`, `BLOB_READ_WRITE_TOKEN`) are set directly in Vercel, independent of the local `.env`. Note the build script (`package.json`) does **not** run `prisma migrate deploy` — only `postinstall: prisma generate`. Applying a schema migration to `prod` has always been a manual step in this project's workflow (`prisma migrate deploy` run by hand against the prod `DATABASE_URL`, same command as for dev, just pointed at the other branch — the CLI pattern used: `vercel env pull <tmpfile> --environment=production`, read `DATABASE_URL` out of it into `$env:DATABASE_URL`, run `prisma migrate deploy`, then delete the tmp file since it holds plaintext prod secrets).

**Phase 5 is live**: pushed to GitHub, migration `20260810015850_product_catalog_v2` applied to `prod`, deployed. Prod's database has **no demo/seed catalog data** — only whatever's been created directly through the live admin (unlike the dev branch, which has the full `seed-demo-content.ts` catalog plus assorted test fixtures, see gap #10). Don't assume prod is empty without checking `/admin` — it's been used directly (real logo uploaded via `/admin/media` or `/admin/settings` at least once, see the note on concurrent-session risk below).

**Phase 6 is live**: migration `20260810082045_phase6_seo_leads_integrations` applied to `prod` and `prisma/seed.ts` re-run against `prod` (adds the new `redirects` RBAC resource) *before* pushing this time — learned from Phase 5's `faqs`-resource gap, so `/admin/redirects` worked on prod on the very first load, no `Forbidden: missing permission` repeat.

**Real bug found live on this deploy, fixed same session**: prod's `NEXT_PUBLIC_SITE_URL` env var carries a leading U+FEFF byte-order mark, which nothing before Phase 6 ever exercised (no code built absolute URLs from it until this phase's SEO work). It silently broke every canonical/hreflang/OG/sitemap/JSON-LD URL in production (`https://.../%EF%BB%BFhttps://...`-shaped garbage). Fixed **defensively in code** (`src/lib/seo/metadata.ts` strips a leading BOM via `String.fromCharCode(0xfeff)`, not a literal invisible character in source — that's deliberate, an invisible codepoint typed directly into a file is a landmine for the next person editing nearby) rather than by editing the Vercel env var, so it's correct regardless of how that variable ever gets set again. The env var itself still has the BOM — harmless now, but worth cleaning up in the Vercel dashboard if anyone's in there anyway.

---

## Phase 7 QA audit

A full build/security/CRUD/i18n/SEO audit, done by actually running the checks and clicking through the real admin/public workflows (browser automation against the dev DB), not just code review. Everything below was verified live; see the final chat report for the complete write-up. Only findings not already covered elsewhere in this file:

- **`saveDraftAction`'s P2028 transaction-timeout bug** (Page Builder save/autosave) — already described under "Homepage architecture" above; applies to any page with enough sections, not just the homepage.
- **RBAC**: all 76 admin server actions across all 14 `actions.ts` files confirmed to call `assertCan`/`can` before any privileged work — no bypasses found.
- **Upload validation gap, fixed**: `saveUploadedFile` (`src/lib/media-upload.ts`) validated PDF/video uploads only by client-supplied `file.type`, never actual content — trivially spoofable. Raster images were already implicitly validated (sharp() throws on non-image bytes); added the equivalent magic-byte check for PDF (`%PDF-` header) and MP4/WebM (`ftyp`/EBML signatures), no new dependency.
- **Missing error boundaries, fixed**: there was no `error.tsx` anywhere in the app — an unhandled runtime error fell back to Next's bare default screen. Added branded boundaries at `src/app/[locale]/error.tsx`, `src/app/admin/error.tsx`, and `src/app/global-error.tsx` (root-layout-failure fallback, self-contained per Next's own guidance since it replaces `<html>`/`<body>` entirely). Verified live by deliberately throwing in a test route.
- **Live breadcrumb JSON-LD bug, fixed**: `src/app/[locale]/solutions/[segment]/page.tsx` built its breadcrumb URLs from the raw `process.env.NEXT_PUBLIC_SITE_URL` instead of the BOM-safe `SITE_URL` export from `src/lib/seo/metadata.ts` — meaning the exact BOM bug documented above (right above this section) was still live on every `/solutions/[segment]` page, just missed when that fix was written. `src/lib/leads/submit-lead.ts`'s admin-notification email had the same raw-env-var pattern (lower severity, internal email only). Both now use `SITE_URL`; grepped the whole `src/` tree afterward to confirm no other call sites do this.
- **Sitemap bug, fixed**: `src/app/sitemap.ts` enumerated every published `Page` row including the reserved homepage row (slug `__homepage__`), emitting a broken `/en/__homepage__` / `/ar/__homepage__` URL pair into the sitemap (that slug 404s by design — see "Homepage architecture"). Excluded it; sitemap entry count dropped from 112 to 110. Also fixed the Menu Builder's "link to page" dropdown showing the raw `__homepage__` slug (cosmetic, that feature isn't consumed by the public site yet — see gap #1).
- **Homepage metadata title bug, fixed**: the new homepage route's `fallbackTitle: "Seven Eleven Trading"` duplicated the site name (`buildMetadata` always appends `" — {siteName}"` to a fallback title), producing "Seven Eleven Trading — Seven Eleven Trading" in the `<title>` tag and Open Graph/Twitter metadata. Fixed to use the existing `nav.home` translation ("Home"/"الرئيسية"), matching how every other route supplies a short fallback title.
- Fixed 13 pre-existing ESLint errors (unrelated to this session's other changes): a real React "mutating a ref during render" bug in `page-builder-shell.tsx` (moved into a `useEffect`), dead `setRevisions` state, and unused imports. Build is now 0 errors / 0 warnings across lint, `tsc --noEmit`, and `next build`.

**Not covered, and why**: no automated test suite exists (`npm run test` isn't a script — pre-existing gap #7, unchanged); no real Lighthouse run (not installed, would require adding a new tool); no testing against actual Safari/Firefox engines (this environment only has one Chromium-based browser available) — CSS in this build uses no vendor-specific hacks, so cross-engine risk is low but unverified; did not exhaustively click through every field of every admin CRUD screen or every breakpoint × locale × page combination — spot-checked representative flows (a full Brand create/update/delete cycle, RBAC via code+agent audit, product/blog/faq/category listing loads) rather than every single one.

---

## Post-Phase-7 work — read this first if continuing this thread

Two sessions' worth of work after Phase 7, tracked in a plan file at `C:\Users\mrahm\.claude\plans\graceful-watching-bonbon.md` (has full section-by-section detail for everything below — read it before starting new work in this area, it's the most precise record). **Deploy status matters here**: session A is live in production; session B is not pushed anywhere yet.

### Session A — Dynamic Menus + Contact page (LIVE in production, commit `779893d`)

Resolved gap #1 (menu wiring) and the equivalent gap for Contact:
- `src/lib/menus.ts` (`getPublicMenu(location, locale)`) resolves the real `Menu`/`MenuItem` records into a tree; `src/components/site/header.tsx`/`footer.tsx` now render from that instead of hardcoded nav arrays (`scripts/seed-menus.ts` seeded HEADER/FOOTER menus matching the old hardcoded structure, both dev and prod).
- `/contact` migrated from a hardcoded route to a real Page Builder page (slug `"contact"`, a normal unique slug — the dedicated route file still wins over the `[...slug]` catch-all for that exact path). `scripts/seed-contact-page.ts` seeded it, both dev and prod.
- `LEAD_FORM`/`CONTACT_FORM`/`QUOTE_FORM` block schema gained `showTypeSelector` (the 4-tab inquiry picker, previously only on the standalone `/contact` route's own component).
- New `CONTACT_INFO` Page Builder block (`blocks/misc/contact-info.tsx` + `contact-info-render.tsx`, the async-Server-Component-render / sync-client-edit split pattern) renders live `SiteSetting`-driven location/email/phone/hours/map — the thing that makes the migrated Contact page's sidebar still work.
- Also fixed in this session: the Page Builder's Arabic/English locale toggle wasn't updating the canvas preview (`editorLocale` state was disconnected from what actually re-rendered) — separate commit `bacdc83`, also live.

### Session B — Ultra Premium visual upgrade + Hero Media System (NOT deployed — verified locally only)

**Two prompts drove this**: first a ~60-section "Ultra Premium Visual Experience Upgrade" brief (cinematic motion, a custom SVG graphic language, kinetic typography, catalog/brand redesigns), then a follow-up "Dynamic Hero Media" brief (full image/video/slideshow CMS system for the Hero block) plus two real product photos the user supplied to use as the default hero image.

**Ultra Premium — all 6 phases complete:**
- **New motion/graphic system**: `src/lib/motion/primitives.tsx` gained `Parallax`, `ClipReveal`, `KineticText` (word-mask reveal), `CountUp`, `DrawLine` (SVG stroke-draw-on-scroll), a magnetic-hover hook, and `CursorLabel` — all gate on `useReducedMotion()`/`prefers-reduced-motion`, several also gate on `(hover: hover) and (pointer: fine)` to no-op on touch. New `src/components/site/graphics/route-line.tsx` + `temperature-indicator.tsx` — an abstract "logistics/cold-chain" SVG motif reused sitewide, RTL-mirrored via the existing `.flip-rtl` convention. Page Builder's `AnimationToken` (`src/lib/page-builder/types.ts`) extended from 4 to 9 values (`fade-down`, `slide-start`/`slide-end`, `scale`, `parallax` added — logical start/end naming, RTL-correct by construction). New `wheat-soft` `BackgroundToken` and `.texture-grain`/`.bg-grid-fine` CSS utilities in `globals.css` for section-rhythm variety without needing photography.
- **AI image generation was attempted and blocked, not skipped by choice**: `google-genai` installed, the user's Gemini API key authenticated fine, but every image-generation request hit a hard `RESOURCE_EXHAUSTED` quota (`limit: 0` for image-gen models on that key's plan — a Google billing/plan issue, not fixable from here). The user's final call: **no generated or stock imagery anywhere** — the whole visual system is typography/graphic-led (`KineticText`, `RouteLine`, `TemperatureIndicator`, texture utilities) rather than photo-led, with every image slot (`Category.imageId`, `Brand.bannerId`, `Hero`'s media fields) fully wired for real photography to drop in later with zero code changes. This is why category cards, the brand wall, mega-menu panels etc. all have an "image-if-present, else graphic/typographic fallback" pattern baked in.
- **New "cold chain journey"** (`src/components/site/cold-chain-journey.tsx`) — the brief's flagship ask: a route line that draws itself via scroll progress with 6 checkpoints activating as they're reached, horizontal on desktop / vertical on mobile, natural scroll (no scroll-jacking). Wired into both the homepage (`TIMELINE` block gained a `layout: "list" | "journey"` field, additive/backward-compatible) and `/distribution-logistics` (replacing its old plain step-list).
- **Catalog/brand surfaces rebuilt**: `CATEGORY_GRID` and `BRAND_GRID` block renders (`blocks/commerce/category-brand-grid-render.tsx`) now do large editorial cards / a typographic brand wall respectively — **deliberately never generate a fake brand logo or fake product photo**, even under the "generate imagery" instruction, since that would fabricate a specific company's identity or misrepresent a real product; brands stay wordmark-only, products keep their honest SKU-text fallback. `product-card.tsx` migrated `<img>` → `next/image`. New `MARQUEE` Page Builder block (`blocks/misc/marquee.tsx` + `marquee-render.tsx`) — loops real brand/category names only, pure CSS animation (no JS loop), hover-pause, auto-frozen by the existing reduced-motion CSS kill-switch.
- **Content pages differentiated**: the 7 `/solutions/[segment]` pages each got a distinct `RouteLine` accent path (not the same icon repeated — see `src/lib/solutions-segments.ts`'s new `accentPath` field) plus `KineticText` headlines; Quality/About pages got the same kinetic-type + graphic treatment; `IconFeatureGrid` (used across About/Solutions/homepage) gained a real desktop hover-active state that deliberately avoids any new fixed icon/text color (a fixed wheat accent already failed WCAG AA here once before, documented in the component) — uses a `bg-current`-based growing underline instead, which is automatically correct against every section tone.
- **Sitewide polish**: footer got a closing `RouteLine` + `KineticText` moment; all three lead-capture forms (`lead-form.tsx`, `contact-form.tsx`, `newsletter-form.tsx`) got real focus-ring/loading-spinner polish; a minimal page-transition fade (`src/app/[locale]/template.tsx`, fade only, no loading splash). Header's "transparent over hero" effect was **deliberately scoped out** (documented in the plan file) — it'd need new cross-layout state plumbing on the one component every page depends on, for an effect that wasn't even visible without a hero photo.
- **A real bug found and fixed via live testing**: the toast confirmation on both `lead-form.tsx` and `contact-form.tsx` only fired on a page's *first* form submission — the `useEffect` depended on `state.success`/`state.error` booleans, which don't change value (so don't re-trigger the effect) on a second submission that resolves to the same outcome. Fixed by depending on the whole `state` object (a fresh reference every submit) instead. Both files fixed, verified live by submitting twice in a row.

**Hero Media System — all 4 phases complete (image, video, slideshow modes + the user's own 28-step test script):**
- **A real pre-existing bug found and fixed, not a new feature**: the Hero's image never actually resolved on a fresh page load — `SectionRenderer` passed raw Zod-parsed data (only ever `imageId`, never a resolved URL) straight to `HeroRender`, a plain sync client component with no way to query Prisma. It only ever *appeared* to work because the admin canvas kept the URL in local React state during an active edit session. Fixed generically: `BlockDefinition` (`src/lib/page-builder/types.ts`) gained an optional `resolveData?: (data) => Promise<data>` hook; `SectionRenderer` (`src/components/site/section-renderer.tsx`) is now `async` and awaits it per section; the admin builder's page loader (`src/app/admin/(builder)/pages/[id]/builder/page.tsx`) applies the same resolution to its initial `dataEn`/`dataAr` fetch. Hero's own hook lives in the new `blocks/content/hero-resolve.ts` (server-only, batches one `prisma.media.findMany`). **Other blocks that reference Media by id via a plain sync Render (`IMAGE`, `IMAGE_TEXT`, `GALLERY`, `LOGO_CLOUD`) are very likely affected by the exact same latent bug** — not fixed, flagged as the highest-value next follow-up.
- **A second real bug found and fixed**: the builder's `saveDraftAction` (`.../builder/actions.ts`) validated each section's data via `dataSchema.safeParse` but then persisted the *original* client-submitted JSON instead of the parsed/stripped result — letting non-schema runtime fields (like Hero's resolved media URLs) leak into permanent storage and go stale. Fixed to persist the Zod-parsed output instead; this is a general data-hygiene fix, not Hero-specific.
- **New Hero schema** (`content-blocks.ts`): `mediaType` (`image`/`video`/`slideshow` — only `image` has real Edit/Render support so far), `layout` (`split` — media in its own column, the original look — or `full-bleed` — media stretches behind the text, added mid-session per the user's request), `desktopMediaId`/`mobileMediaId`/`posterId`, `imagePosition`/`focalX`/`focalY` (numeric X/Y, not a drag-picker, matching the user's own admin-UX mock), `overlayOpacity`, `animation` (`none`/`fade`/`slow-zoom` default/`parallax`/`reveal`/`cinematic`), `videoAutoplay`/`Muted`/`Loop`, and per-button `ctaVisible`/`ctaStyle`/`ctaExternal`. `slides: []` is schema-ready for Phase C but has no Edit UI yet.
- **The real default hero image, live**: the user supplied two screenshots (full homepage mockups with baked-in fake nav/text/stats — not usable as-is, would have duplicated the real dynamic header/hero-text rendered on top). Cropped to pure photography (Golden Seven French fries + Totapuri mango pulp product packaging + the delivery truck + a world-map overlay graphic, no baked UI) via a one-off Pillow script — saved at `hero-crop-English.png`/`hero-crop-Arabic.png` in the session's scratchpad temp dir (not committed to the repo; re-crop from the originals at `C:\Users\mrahm\OneDrive\Desktop\711\English.png`/`Arabic.png` if needed again, crop box `(740, 105, 1672, 775)` on the 1672×941 source). Uploaded through the *real* Media Library pipeline (`scripts/seed-hero-media.ts`, calls the actual `saveUploadedFile` → Vercel Blob, not a shortcut) — one image per locale, real `Media` rows visible in `/admin/media`. `scripts/seed-homepage.ts`'s HERO section now references these by id.
- **Verified live (Phase A)**: fresh (non-cached, no prior edit session) loads of the homepage in both `/en` and `/ar`, and a fresh admin builder load, all correctly show the real photo — proving the resolution-bug fix, not just that the schema changed. Full-bleed layout toggled live in the admin and confirmed rendering correctly.
- **Phase B — video mode**: `HeroEdit` gained the desktop/mobile video pickers, poster picker, and autoplay/muted/loop checkboxes (previously a "coming soon" placeholder); `HeroRender` gained a real `<video>` layer (`HeroVideoLayer`, in the new `hero-shared.tsx`) with a poster-image fallback on error. Two real bugs found and fixed while testing: (1) switching `mediaType` didn't clear the previous mode's media ids, so a leftover image id could show as "already selected" in the video picker and then fail to play as a video source — now cleared on an actual mode change; (2) a server-rendered `<video src>` can start fetching (and fail — a 404, a CSP-blocked host) *before* React finishes hydrating and attaches `onError`, so a pre-hydration failure went silently undetected — fixed with a `ref` callback that checks the already-set `.error` the moment the DOM node commits, catching the case `onError` alone misses. Verified live: fresh-load autoplay/muted/loop/poster all correct, the 404/CSP fallback path swaps to the poster correctly, mobile/desktop split works, autoplay-off checkbox respected.
- **Phase C — slideshow mode**: new `hero-slides-edit.tsx` (`HeroSlidesEditor`, a collapsible per-slide card list — media/content/CTAs/duration/animation — with add/remove/duplicate/reorder) and `hero-slideshow-render.tsx` (`HeroSlideshow` — auto-advances on a timer, pauses on hover matching `MARQUEE`'s precedent, only ever mounts the *active* slide's media, dot indicators for manual navigation, content rotation stays functional under reduced motion since only each slide's entrance animation is what reduced-motion suppresses). `HeroFrame`/`HeroMediaMotion`/`HeroVideoLayer` were pulled out of `hero.tsx` into a new `hero-shared.tsx` specifically to avoid a circular import between `hero.tsx` (which dispatches to `HeroSlideshow` for slideshow mode) and the new slideshow renderer (which reuses those same three pieces). Verified live: added real slides in the admin, watched it auto-advance on schedule (confirmed via timestamped console tracing after an initial round of manual checks was thrown off by MCP tool round-trip latency landing near-periodically on the same slide — not a real bug, a testing-tool artifact), hover correctly pauses and un-hover correctly resumes, dot-click navigation works, only the active slide's media (never all slides at once) is ever in the DOM.
- **Phase D — the user's own 28-step end-to-end test script**: walked live in the admin (image replace → Save Draft → confirm public site unchanged → Publish → confirm it changed → video mode → back to image → mobile image → focal point crop → animation preset → EN content + `/en` → AR content + `/ar` → Revision History restore → verify restoration), then a final `npm run build`. **A third real bug found and fixed during this pass**: `restoreRevisionAction` (`.../builder/actions.ts`) ran its safety-snapshot-then-restore sequence as N sequential `tx.pageSection.create()` calls inside one interactive `$transaction` with no explicit timeout — the exact same P2028 transaction-timeout failure class already fixed in `saveDraftAction` (see "Homepage architecture" above), just never applied to Restore. Reproduced live restoring the homepage's 12-section homepage (an `ErrorBoundaryHandler` catch, `PrismaClientKnownRequestError: Invalid tx.pageRevision.create() invocation ... transaction timeout`); the transaction rolled back cleanly (confirmed via direct DB read — no partial/orphaned rows), so the failure was safe, just broken. Fixed identically: the sequential creates became one `tx.pageSection.createMany()`, plus an explicit `{ timeout: 20000 }` on the transaction. Re-tested live immediately after the fix — restore now succeeds, the draft correctly reverts (confirmed via DB read of the exact field values, not just the UI), the public site correctly stays on the pre-restore content until a subsequent Publish (matching the documented "restore doesn't auto-republish" behavior), and publishing afterward correctly makes the restored content live in both locales. `npx eslint .` (whole project) and `npm run build` both clean after the fix.
- **Homepage left in its clean, real-content state** after all four phases' live testing: Hero back to image mode, the real EN/AR default photos, center focal point, `slow-zoom` animation, no test slides, no leftover crop overrides — same state a Phase-C cleanup pass had already reached, re-confirmed via DB read after Phase D's testing. Test video fixtures uploaded for Phase B/D's own testing (a ~580KB Windows system sample video, not project content — same throwaway-QA-fixture precedent as the test SVG in gap #10) were deleted afterward via the real delete pipeline (`deleteUploadedFile` + `prisma.media.delete`), not left behind.

### Session B continued — Premium Cinematic Hero System (NOT deployed — all 4 phases done, verified live)

**Same thread as session B above, later prompt.** The user supplied a huge (59-section) follow-up brief asking for a full Hero redesign: an organic image-frame/mask system instead of a rectangular card, decorative typography, pointer parallax, lighting/shadow, and a new **Product Composition** Hero mode that composites up to three real Products from the catalog. Full detail (context, design decisions, phase-by-phase plan) is in the plan file at `C:\Users\mrahm\.claude\plans\snappy-stirring-papert.md` — **read that before continuing this work**, this section is a compressed status summary only.

**Blocking fact surfaced during recon, already resolved with the user via `AskUserQuestion`**: the real Product catalog (23 products — Whole Frozen Chicken, Salmon Fillet, Basmati Rice, Sunflower Oil, etc.) has **zero products with any photo uploaded**, and no "Golden Seven"-named products exist in the CMS at all ("Golden Seven" is packaging visible only inside the two existing flat hero photos). User's choice: build Product Composition mode's full CMS architecture now (real references, live-updating, no fabrication) and ship the new frame/typography/parallax system on **Image mode** first, since that already has real photography.

**Phase 1 — Frame/lighting/depth/typography/parallax system (done, verified live):**
- New `src/lib/page-builder/blocks/content/frame-shapes.ts`: `FRAME_STYLES` (15 shapes — rounded-rectangle, soft/vertical/horizontal oval, organic-blob ×3 variants, arch, soft-arch, curved-rectangle, diagonal-cut, wave, capsule, asymmetric-curved, full-bleed), all as percentage-based `border-radius`/`clip-path` (never `clip-path: path()`, which uses fixed px-like units and doesn't scale with the box — confirmed this matters, not a hypothetical). `FRAME_PRESETS` (8 named bundles — Elegant Oval, Organic Premium, Editorial Arch, Asymmetric Wave, Luxury Curved, Full Bleed Product, Saudi Premium, **Golden Seven Signature** — the bespoke navy/teal/gold organic-asymmetric-blob brand frame, §21 of the brief) that fill several `heroSchema` fields at once but stay fully editable after, the exact same precedent as the pre-existing `POSITION_PRESETS` pattern in `hero.tsx`.
- `src/lib/motion/primitives.tsx` gained `usePointerParallaxContainer()` (tracks normalized pointer position within a container, one shared listener) + `pointerParallaxStyle(x, y, depthPx)` (per-layer GPU-friendly `translate3d`) — a genuinely new primitive, since the pre-existing `Parallax` is scroll-linked, not pointer-linked. Also exported the two previously-internal gating hooks `useIsRtl`/`useFinePointer`.
- `heroSchema` (`content-blocks.ts`) gained: `frameStyle`/`mobileFrameStyle`/`framePreset`, `framePosition`/`frameX`/`frameY`/`frameWidth`/`frameHeight`/`frameScale`/`frameRotation`/`frameOverflow`, `frameBorderStyle`/`Width`/`Opacity`/`Color`, `frameGlow`, `decorativeText`/`decorativeOpacity`/`decorativePosition`/`decorativeRotation`, `parallaxEnabled` — all additive/defaulted (`frameStyle` defaults to `"full-bleed"`, i.e. today's plain rectangular look, so every already-published Hero renders unchanged until an admin picks a shape). `HeroAnimation` extended with `scale`/`morph`/`float`.
- New `hero-frame-shape.tsx` (`HeroFrameShape`): layers same-shaped glow/shadow/border divs behind the content (never a plain CSS `border` on a `clip-path` element — confirmed live that doesn't compose, the border draws along the box's rectangular edge then gets unevenly clipped) plus an optional `border-radius` morph animation (framer `animate`, not hand-written CSS keyframes) for the 3 organic shapes. New `hero-decorative-typography.tsx` (`HeroDecorativeTypography`) — the oversized low-opacity brand-text layer, `aria-hidden`, renders nothing if `decorativeText` is empty (never invented copy).
- Wired into `HeroEdit`/`HeroRender` for `mediaType === "image" && layout === "split"` only (full-bleed layout and video mode untouched this phase, matches the plan).
- **Two real bugs found and fixed live, both in `hero-frame-shape.tsx`**: (1) a bare numeric `inset` value in an inline style — React doesn't auto-append `px` to `inset` — rendered as invalid CSS, silently collapsing the box; (2) an inline `style={{position:"relative"}}` on the outer div silently overrode the caller's `absolute` Tailwind class (inline styles beat classes in the cascade), collapsing the whole frame to near-zero size since all its children are absolutely positioned. Both confirmed fixed via `getComputedStyle` (real 614×490px organic-blob shape, image visible, network 200).
- **Important environment finding, not an app bug**: this session's browser automation pane has `document.visibilityState` stuck at `"hidden"` (confirmed via a direct `requestAnimationFrame` loop that never completed, and `document.hasFocus()`/`visibilityState` checks) — so every framer-motion `animate`/`whileInView` animation on the page stays visually frozen at its initial ("hidden") state in this tool specifically, including the *pre-existing* `KineticText` headline reveal that shipped before this session. Confirmed via `getComputedStyle`/DOM inspection instead (which is unaffected) that the underlying markup/CSS is correct. If a future session hits "my animation looks stuck" in this same browser tool, check `document.visibilityState` before assuming a code bug.

**Phase 2 — Product Composition mode (done, verified live with real catalog data):**
- `resolveData` (the `BlockDefinition` hook that lets Hero stay a sync client component while resolving server-side references) now takes `(data, locale)` — `locale` added because a Product's name is per-locale (`ProductTranslation`); updated the 2 call sites (`section-renderer.tsx`, builder `page.tsx`'s `resolveSectionData`, called once as `"en"` and once as `"ar"`).
- `reference-data-context.tsx` gained `products: ReferenceOption[]`, eager-loaded in the builder `page.tsx` loader (23 rows, same order-of-magnitude eager-load pattern already used for categories/brands, not a new search component).
- `heroSchema`: `mediaType` gained `"product-composition"`; `primaryProductId`/`secondaryProductId`/`supportingProductId` (each optional — empty = that role doesn't render), `productsClickable`, `showProductBadges`.
- `hero-resolve.ts`'s `resolveHeroData` now also batch-queries `prisma.product.findMany` for the 3 ids when present, attaching `primaryProduct`/`secondaryProduct`/`supportingProduct` (`{id, name, slug, sku, imageUrl, isFeatured}`) — real, always-current catalog data, never duplicated into Hero's own JSON.
- New `hero-product-composition-render.tsx` (`HeroProductComposition`): layers the 3 roles (primary largest/foreground z-30, secondary z-20, supporting z-10) using Phase 1's `HeroFrameShape`, a per-role floating idle animation, real `next/image` when `product.imageUrl` exists else the **exact same honest name/SKU text fallback** `ProductCard`/`CATEGORY_GRID`/`BRAND_GRID` already use (since 0 products have photos today — see the blocking fact above), `<Badge tone="featured">` (the project's one badge component, reused not reinvented) gated on the real `Product.isFeatured` boolean, click-through via the existing `resolveHref` pattern.
- **Verified live end-to-end** with real products (Salmon Fillet / Basmati Rice / Sunflower Oil): fresh admin canvas load and published public site both show the 3 real names/SKUs; toggled `isFeatured` on Salmon Fillet directly via a Prisma script (mirroring what `/admin/products` would do) and confirmed the Featured badge appeared/disappeared on the Hero **without touching Hero config at all** — proves the "store references, not data" architecture; confirmed click-through links resolve to the real `/en/products/salmon-fillet-skin-on` etc.
- **A deploy-order bug found live**: a Publish click didn't take effect the first time (draft was correct, published revision wasn't updated) — caught by reading the actual published DB row rather than trusting the UI, not investigated further (re-clicking Publish worked), possibly just needed more wait time between Save and Publish. Worth a beat of caution if a Publish ever seems to silently not apply — verify via DB read, don't assume.
- **`Badge tone="featured"` initially hardcoded the English word "Featured"** instead of using the existing `productCard.featured` i18n key `ProductCard` already uses — fixed to `useTranslations("productCard")` + `t("featured")` before this was ever verified on `/ar` (caught in self-review, not live-discovered, so flagging in case the AR-specific check gets missed later).

**Phase 3 — Mobile composition + cold-chain graphic + accessibility + RTL polish (done, verified live):**
- `HeroFrame` (`hero-shared.tsx`): mobile composition order fixed via CSS `order` (`content` gets `order-2 lg:order-1`, the media slot gets `order-1 lg:order-2`) so single-column mobile shows Background/Product → Text → CTA (brief §42), not the plain top-to-bottom DOM stack (which put text above the image) — `content` stays first in DOM at every breakpoint for the pre-existing RTL grid-track-1-is-inline-start reasoning documented in the code; only the *visual* order changes on mobile. **Verified live**: fresh `/en` load at a real 375×812 mobile viewport shows image → decorative typography → body text → CTA, matching spec.
- `mobileFrameStyle` schema field added (empty string = same as desktop). `HeroRender`'s framed-image branch now renders **two separate** `HeroFrameShape` instances (desktop `hidden lg:block`, mobile `lg:hidden`), each with its own frame style, instead of one shape wrapping two `<Image>`s — needed so mobile can genuinely use a different shape (brief §43, e.g. desktop Blob / mobile Oval). **Verified live**: set to `"capsule"` while desktop stayed `"organic-asymmetric-blob"`, published, confirmed via DOM inspection the two breakpoints render genuinely different `border-radius` formulas (not just a scaled copy), then reverted to the clean "same as desktop" default and republished.
- Added one `TemperatureIndicator` cold-chain accent (reusing the pre-existing component from `cold-chain-journey.tsx`'s toolkit, no new SVG) near the frame, extremely subtle (`text-wheat/40`), gated on `frameGlow !== "none"` so it only appears alongside an actual premium frame treatment, not as sitewide clutter. **Verified live**: present (real thermometer SVG, `aria-hidden`) on `/en` desktop where `frameGlow: "gold"`; correctly `display:none` at mobile width (`hidden lg:block`); correctly absent entirely on `/ar` where `frameGlow: "none"`.
- Accessibility fixes made during self-review: Product Composition's real-photo branch now sets `alt={product.name}` (was `alt=""` with no adjacent visible text in that branch, unlike the no-photo fallback which shows the name as text) so screen readers get a meaningful description either way. **Verified via code review**: every decorative layer (frame glow/shadow/border, decorative typography, cold-chain accent) is `aria-hidden`; Hero content never depends on animation to be visible (`Stagger` renders `initial="show"` under reduced motion, `HeroMediaMotion`/`HeroFrameShape`'s morph both no-op under reduced motion); CTA/product links are native `<a>`/`<button>` elements, confirmed keyboard-reachable with a visible focus ring via a live Tab-key walk to the Hero's "Request a quote" CTA.
- **RTL pass, done live on `/ar`**: confirmed `dir="rtl"`; confirmed `HeroDecorativeTypography` uses Tailwind's logical `-start-`/`-end-` positioning (mirrors correctly by construction); confirmed pointer parallax is geometry-relative (pointer position within the container), not text-direction-relative, so it needs no RTL-specific handling. **One real thing found and fixed**: `hero.tsx`'s `FRAME_POSITION_ALIGN` had a code comment claiming "Frame position: Left/Right" was deliberately *physical* (screen-left always), matching `imagePosition`'s convention — live testing (temporarily setting AR's `framePosition` to `"left"`, publishing, reading real `getBoundingClientRect()`s) showed the actual behavior is the opposite: Tailwind's `justify-start`/`justify-end` on a flex row is inherently direction-relative, so "Left" correctly anchors to the RTL-start (visually right) side, mirroring like the rest of the split layout. That's the *correct* behavior (matches this plan's own Phase 3 acceptance criteria of "frame position uses logical start/end") — the comment was simply stale/inaccurate, now corrected in place; no functional change was needed. Test values were reverted and republished after confirming.
- **Reduced-motion pass**: verified via code review (this environment's browser-automation pane can't emulate `prefers-reduced-motion` live — see the `document.visibilityState`-stuck-hidden environment note above, a related but distinct limitation) — every new animated surface (`HeroMediaMotion`, `HeroFrameShape`'s morph, Product Composition's floating idle) calls `useReducedMotion()` and no-ops correctly.

**Phase 4 — user's own end-to-end test pass + production build (done):** the literal 52-step script from brief §58 isn't saved anywhere accessible to this session (only referenced by section number in the plan file, not reproduced as text) — ran an equivalent representative regression pass instead covering every area the plan itself calls out: pointer parallax confirmed live (real `translate3d` offset changes with pointer position on desktop, confirmed absent under mobile/touch `matchMedia` emulation), keyboard nav confirmed (Tab reaches the Hero CTA with a visible focus ring), a real Save → Publish → verify-unchanged-then-changed round trip (multiple times, across the `mobileFrameStyle` and `framePosition` tests above), and a real Revision History → Restore cycle (restored an older revision, confirmed via direct DB read that the **draft** changed but the **published** snapshot did not — proving "restore doesn't auto-republish" still holds — then corrected the draft back to the clean state and republished). Frame style/preset and position/scale/rotation controls were spot-checked (not exhaustively combinatorial across all 15 shapes × 8 presets — matches this project's established "representative flows, not every single one" QA precedent from Phase 7). Final `npx tsc --noEmit`, `npx eslint .`, and `npm run build` are all clean (0 errors/warnings, 46 routes built successfully).

**Not measurable in this environment** (pre-existing, documented gaps, not new to this pass): no Lighthouse/Core Web Vitals (not installed); no real `prefers-reduced-motion` OS-level emulation (browser-automation tool has no such control); no cross-engine (Safari/Firefox) testing (only one Chromium-based browser available here).

**New files this pass** (all under `src/lib/page-builder/blocks/content/` unless noted): `frame-shapes.ts`, `hero-shared.tsx` (pulled `HeroFrame`/`HeroMediaMotion`/`HeroVideoLayer` out of `hero.tsx` to avoid a circular import with the slideshow renderer — this predates the frame system, from the Hero Media System work), `hero-frame-shape.tsx`, `hero-decorative-typography.tsx`, `hero-product-composition-render.tsx`. Modified: `hero.tsx` (now large — Edit UI for every mode plus Render dispatch), `content-blocks.ts` (`heroSchema`), `hero-resolve.ts`, `types.ts` (`resolveData` signature), `section-renderer.tsx`, builder `page.tsx`, `reference-data-context.tsx`.

**Current live state of the dev homepage Hero**: `mediaType: "image"`, `framePreset: "golden-seven-signature"` (organic-asymmetric-blob shape, gold gradient border, gold glow, `decorativeText: "SEVEN"`), the real EN hero photo, `animation: "fade"` (leftover from Phase 1 debugging — cosmetically fine, just not the preset's own default `"cinematic"`, worth resetting if that matters later). `primaryProductId`/`secondaryProductId`/`supportingProductId` are still set (Salmon/Basmati/Sunflower) from Phase 2 testing even though `mediaType` is back to `"image"` — harmless (Product Composition-only fields are simply unused while `mediaType !== "product-composition"`), same "leftover config on unused fields" pattern already established elsewhere in this schema (e.g. `slides: []` persisting outside slideshow mode).

### Known loose ends from session B (not yet resolved)

- **The Premium Cinematic Hero System is fully complete (all 4 phases done and verified live)** — see immediately above. Nothing further is planned in this specific thread; the homepage Hero is left in the same clean state documented below, re-confirmed via DB read after this pass's testing.
- **Nothing from session B is pushed to GitHub or deployed.** Only session A (`779893d`, `bacdc83`) is live. If asked to deploy, confirm with the user first (per this project's established pattern — every deploy so far has been a separate explicit instruction) and expect to reseed prod's DB the same way dev was seeded (`seed-homepage.ts`, `seed-hero-media.ts` before it) — same `vercel env pull` + override-`$env:DATABASE_URL` pattern as every other prod script in this project.
- **The same "media never resolves on a fresh load" bug is very likely present in `IMAGE`, `IMAGE_TEXT`, `GALLERY`, and `LOGO_CLOUD`** (any block referencing `Media` by id via a plain sync `Render`, not an async-Server-Component one) — confirmed as a real architectural risk, not yet audited or fixed for those specific blocks.
- **Throwaway test leads in the dev DB** from forms QA testing this session: `QA Test`, `QA Test 2`, `QA Test 3`, `QA Test 4`, `Toast Fix Test 1`, `Toast Fix Test 2` (plus the pre-existing ones already documented in gap #10 below). Flagged to the user, not deleted (no lead-delete admin action exists, matching CRM semantics — see gap #10).
- **`GEMINI_API_KEY` was added to `.env.local`** (gitignored) for the abandoned AI-image-generation attempt — still present, unused, harmless, but real (it's the user's actual key, pasted directly in chat — don't echo it back if this file or the key ever comes up again).
- **The `/products/[slug]` intermittent-hang issue** flagged much earlier in this project's history (root-caused to a pre-existing `Suspense`/`loading.tsx` issue on that route, confirmed via git-stash A/B to predate any of this session's changes) was never revisited or reconfirmed — status unknown, low-signal, mention only if it resurfaces.

---

## How to resume work

```bash
# from D:\Claude Code
git log --oneline -5          # should be at or after commit bacdc83 (locale-toggle fix, session A's last push)
git status                    # session B's work (Ultra Premium + Hero Media System + Premium Cinematic Hero System) is uncommitted if it's still there — check `git status` and this file's "Post-Phase-7 work" section before assuming a clean tree means nothing's pending
npx prisma migrate status     # dev should show all 8 migrations applied (no new migrations from session B -- it only touched Page Builder JSON data, no schema changes)
```

If continuing the **Premium Cinematic Hero System** specifically: read `C:\Users\mrahm\.claude\plans\snappy-stirring-papert.md` for full design-decision detail, then this file's "Session B continued" section above for exactly what's done/verified vs. not. Sanity-check the working tree still matches what's described (`npx tsc --noEmit && npx eslint .` should both be clean) before resuming — a stale/corrupted `.next` directory has caused a false typecheck failure once already this session (fix: `rm -rf .next` then rebuild).

To run the dev server (PowerShell, from this repo):
```powershell
$env:Path += ";C:\Program Files\nodejs;$env:APPDATA\npm"
npm run dev
```
Or use the `preview_start` tool with config name `seven-eleven-trading` (dev) or `seven-eleven-trading-prod` (build + start; run `npm run build` first).

Log in at `/admin/login` with the credentials in `.env`.

If `prisma generate` seems out of sync after a fresh checkout: `npx prisma generate`. If schema changes are needed, follow the migration workaround documented above (not `prisma migrate dev`).

---

## Natural next steps (not started, just candidates)

**Most likely next, given where session B left off:**
- **Push and deploy session B** (Ultra Premium redesign + the 4-phase Hero Media System + the now-complete 4-phase Premium Cinematic Hero System) — everything is verified locally but sitting uncommitted/unpushed. Needs an explicit user go-ahead first (established pattern), then the usual prod-DB reseed dance (`seed-homepage.ts`, `seed-hero-media.ts` before it).
- **Audit `IMAGE`/`IMAGE_TEXT`/`GALLERY`/`LOGO_CLOUD` for the same "media never resolves on a fresh load" bug** that Hero had — high-confidence latent bug, not yet checked.

**Older candidates, still open:**
- Build a dedicated public certifications page once real certification data exists (admin UI is ready, seeded empty by design)
- Custom role/permission builder UI
- Consume remaining `SiteSetting` fields on the public site (logo/favicon replacing the text wordmark, business hours display)
- Automated test suite
- Clean up or keep the test data listed above, including the new `/page-builder-e2e-test` page, the leftover test SVG, and the growing pile of test leads (ask the user)
- Supply real GA4/GTM/Meta Pixel IDs, a real WhatsApp number, a real Google Maps embed URL, and real SMTP credentials via `/admin/settings` / env vars whenever the user has them — everything is wired and tested, just waiting on real values (never invented, per instruction)
